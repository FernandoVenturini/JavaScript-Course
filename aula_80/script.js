const mat = document.getElementById('mat'); 

//mat.innerHTML = Math.E;
//mat.innerHTML = Math.random();
//mat.innerHTML = Math.floor(Math.random() * 10);
const num = Math.random() * 10; // Mostra numeros ate 1;
//mat.innerHTML = Math.floor(num); // Mostra numeros ate 9;
//mat.innerHTML = Math.round(num);// Mostra numeros ate o 10;
//mat.innerHTML = Math.sqrt(64); // Motra raiz quadrada;
mat.innerHTML = Math.pow(10, 3);